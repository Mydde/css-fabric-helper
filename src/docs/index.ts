import json2md from "json2md";
/* import cssFabricHelper, { ICssFabricProps } from "css-fabric-helper";

const examples: { [key: string]: any } = {};

examples["test"] = {
  padding: 2,
  border: { bottom: 2, top: 3 },
};

examples["test"] = {
  padding: "",
  border: { bottom: 2, top: 3 },
};

const exList: any = Object.keys(examples).map((cssObject2Key, index) => {
  let cssObject2 = examples[cssObject2Key];

  let red = JSON.stringify(cssObject2, null, "\t");
  let reds = cssFabricHelper.process(cssObject2);

  return { balise: "ouverte" };
});

console.log(json2md(exList)); */

const makeDocs = ()=>{}
export default makeDocs;
